{
  "name": "openclaw-cloud-backend",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-ecs": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "@lemonsqueezy/lemonsqueezy.js": "^3.2.0",
    "ulid": "^2.3.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.160",
    "@types/node": "^20.0.0",
    "typescript": "^5.9.3"
  }
}
